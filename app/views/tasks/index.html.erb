<h1>My Tasks</h1>
<% @tasks.each do |task| %>
<input type="checkbox" name="complete-status" value="task">
<label for="complete-status"><%= link_to task.title, task_path(task) %></label><br>
<% end %>

<button type="button" class="btn btn-light"><%= link_to "+ Create New", new_path%></button>
