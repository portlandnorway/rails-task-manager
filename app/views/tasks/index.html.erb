<h1>My Tasks</h1>
<% @tasks.each do |task| %>
<input type="checkbox" name="complete-status" value="task">
<label for="complete-status"><%= task.id %>. <%= link_to task.title, show_path(task) %></label><br>
<% end %>
